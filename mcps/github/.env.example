# GitHub OAuth App — used by both the CLI and the MCP server
# (shared browser PKCE flow via the auth proxy, with device-code fallback)
GITHUB_CLIENT_ID=<your-github-oauth-app-client-id>
GITHUB_CLIENT_SECRET=<your-github-oauth-app-client-secret>
