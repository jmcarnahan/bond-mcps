# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
*.egg-info/
dist/
build/

# OS
.DS_Store

# Secrets / local env
.env
.env.local

# Token database (encrypted at rest, but still excluded from VCS)
tokens.db
tokens.db-shm
tokens.db-wal
.bond_mcps_encryption_key

# Dev orchestration scratch (Makefile writes logs here)
tmp/

# Git worktrees
.worktrees/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
*.tfstate.lock.info
# But: keep the per-env templates checked in.
!deployment/terraform-existing-vpc/environments/*.tfvars

