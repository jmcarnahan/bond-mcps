# Aurora Postgres Serverless v2 cluster: aurora-postgresql 15.12, KMS-encrypted,
# parameter group with rds.force_ssl=1, subnet group across private subnets,
# random master password, db_credentials secret shell created in secrets.tf.
