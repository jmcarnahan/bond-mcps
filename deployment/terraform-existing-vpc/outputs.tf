# Module outputs: cluster_name, cluster_endpoint, ecr_repository_urls (map),
# service_urls (map), auth_proxy_url, service_internal_dns (map),
# aurora_writer_endpoint (sensitive), aurora_secret_arn, shared_alb_group_name,
# needs_scaling_work_services (list).
