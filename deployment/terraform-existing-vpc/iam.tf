# IAM baseline: ESO IRSA role (Secrets Manager + KMS decrypt scoped to bond-mcps-${env}-*),
# AWS Load Balancer Controller IRSA role, per-service IRSA placeholder (only needed if a
# service requires AWS API access beyond what ESO provides).
